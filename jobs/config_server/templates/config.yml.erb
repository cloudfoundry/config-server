<%=
---
port: p('config_server.port')
database:
  adapter: p('config_server.db.adapter')
  user: p('config_server.db.user')
  password: p('config_server.db.password')
  host: p('config_server.db.host')
  port: p('config_server.db.port')
  database: p('config_server.db.database')
  connection_options:
    max_connections: p('config_server.db.connection_options.max_connections')
    pool_timeout: p('config_server.db.connection_options.pool_timeout')
%>
